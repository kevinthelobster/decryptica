import SubmitClient from './SubmitClient';

export const metadata = {
  title: 'Submit a Prompt — OpenClaw Prompt Library',
  description: 'Share your OpenClaw automation setup with the community. Submit prompts for review and inclusion in the prompt library.',
};

export default function SubmitPage() {
  return <SubmitClient />;
}
